## 🎯 目的 / 実装詳細 (To-Be)
新生 Qraft の全機能を支えるための Drizzle ORM スキーマを再定義し、WBS・分析・報告の基盤を構築する。

---

## 📊 検証計画 & 判定基準 (Verification Plan)

| No. | 検証項目 | 実施手順 | 期待値（「正しい」の定義） | 提出エビデンス |
| :--- | :--- | :--- | :--- | :--- |
| **01** | **WBS 自己参照構造** | 親 A と子 B をインサートし、`with: { children: true }` で取得。 | `(A).children[0].id === (B).id` であること。 | ①Vitest実行ログ<br>②DBダンプ(JSON) |
| **02** | **FK 制約の有効性** | 存在しない `parent_id` を指定して子タスクをインサート。 | `SQLITE_CONSTRAINT_FOREIGNKEY` エラーで保存失敗すること。 | エラーログの<br>スクリーンショット |
| **03** | **型バリデーション** | `estimated_hours` に数値以外（"abc"等）を送信し保存試行。 | `400 Bad Request` で保存が拒否されること。 | APIレスポンスの<br>スクリーンショット |
| **04** | **分析カラムの実装** | `defects` テーブルに `cause_category` 等の新カラムが反映されているか確認。 | `npx drizzle-kit push` が正常終了し、DBにカラムが存在すること。 | Migration成功ログ |

---

## ✅ 完了条件 (Acceptance Criteria)
- [ ] 上記 No.01 〜 04 の全エビデンスが本 Issue のコメントに添付されていること。
- [ ] エビデンスが「判定基準」の内容と 100% 一致していること。

---
> [!CAUTION]
> **エビデンス不備による差し戻し**
> 表中の期待値が物理的に確認できない証跡（ボケた画像、不完全なログ等）は、未完了として差し戻します。
